#!/bin/sh

# Prefer the fastest linker available without making it a prerequisite for
# contributors or CI machines that only have the platform C compiler.
if command -v clang >/dev/null 2>&1 && command -v mold >/dev/null 2>&1; then
    exec clang -fuse-ld=mold "$@"
fi

exec cc "$@"
